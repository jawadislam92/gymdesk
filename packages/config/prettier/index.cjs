/**
 * Shared Prettier configuration for GymFlow Suite.
 * Consumed at the repo root via `.prettierrc.cjs` and by individual packages.
 */
module.exports = {
  printWidth: 100,
  tabWidth: 2,
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  bracketSpacing: true,
  arrowParens: 'always',
  endOfLine: 'lf',
};
